<p id="notice"><%= notice %></p>

<p>
  <strong>Body:</strong>
  <%= @question.body %>
</p>

<p>
  <strong>Project:</strong>
  <%= @question.project_id %>
</p>

<p>
  <%= @question.body %>
</p>
<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Response</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @question.answers.each do |answer| %>
      <tr>
        <td><%= answer.body %></td>
        <td><%= link_to 'Update', edit_project_question_path(@project, @question) %></td>
        <td><%= link_to 'Remove', [@project, @question], method: :delete, data: { confirm: 'Are you sure you want to delete this question?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Edit', edit_project_question_path(@project, @question) %> |
<%= link_to 'Back', project_questions_path(@project) %>
